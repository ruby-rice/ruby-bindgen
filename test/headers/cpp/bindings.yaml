format: Rice

# In tests, each test method sets match: to specify which header(s) to process.
# This mirrors how bin/ruby-bindgen uses match patterns from the config.
match:

clang-cl:
  args:
    - "-xc++"
    - "-std=c++17"

clang:
  args:
    - "-xc++"
    - "-std=c++17"
